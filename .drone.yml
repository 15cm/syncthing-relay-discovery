---
kind: pipeline
type: docker
name: docker-build

steps:
  - name: docker
    image: 15cm/drone-kaniko
    settings:
      repo: 15cm/syncthing-relay-discovery
      auto_tag: true
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password

trigger:
  branch:
    - master
  event:
    - push
    - custom
    - tag
